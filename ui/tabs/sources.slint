// Custom window sources tab
import { VerticalBox, ScrollView, Button } from "std-widgets.slint";

export component SourcesTab {
    // TODO: Add sources management properties and callbacks
    
    ScrollView {
        VerticalBox {
            Text {
                text: "Custom Window Sources";
                font-size: 18px;
                font-weight: 700;
            }
            
            Text {
                text: "TODO: Implement sources management controls";
            }
        }
    }
}
