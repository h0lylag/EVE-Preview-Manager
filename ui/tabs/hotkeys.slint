// Hotkey configuration tab
import { VerticalBox, ScrollView, Button, CheckBox } from "std-widgets.slint";

export component HotkeysTab {
    // TODO: Add hotkey settings properties and callbacks
    
    ScrollView {
        VerticalBox {
            Text {
                text: "Hotkey Settings";
                font-size: 18px;
                font-weight: 700;
            }
            
            Text {
                text: "TODO: Implement hotkey configuration controls";
            }
        }
    }
}
